/**
 * Configurações globais da aplicação.
 * Valores que podem mudar entre ambientes devem vir de variáveis de ambiente (import.meta.env).
 */

export const APP_NAME    = 'Finanças Pessoais'
export const APP_VERSION = '1.0.0'
export const LOCALE      = 'pt-BR'
export const CURRENCY    = 'BRL'

/** Tamanho máximo do avatar após redimensionamento (pixels). */
export const AVATAR_SIZE = 200

/** Qualidade JPEG do avatar comprimido (0–1). */
export const AVATAR_QUALITY = 0.7
